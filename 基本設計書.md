# 基本設計書: マルチローカルLLMベンチ & 多段チューニング研究基盤

バージョン: v2 (旧 NPC 会話特化 v1 からの全面改訂)
更新日: 2025-01-XX

## 1. 文書目的 / スコープ
目的: 複数のローカル LLM (7B〜70B) を異なるランタイム・量子化形式・チューニング手法で統一的に
評価・比較・改良 (SFT→効率化適用→手法比較→RL系) できる再現性指向の研究基盤の基本設計を定義する。
対象範囲:
- モデル/ランタイム/量子化 抽象化
- チューニング/評価/ジャッジ/比較パイプライン
- コンフィグ体系・ログ/アーティファクト管理
- プロンプト適応層 & Structured 出力整形 (任意タスク向け)
- メトリクス / コスト & 品質複合スコアリング
- セキュリティ / ライセンス遵守 / 再現性

非対象 (将来拡張): 分散ノード間同期、エージェントオーケストレーション、MLOps 本格運用。

## 2. コアコンセプト
1) Configuration as Experiment (Notebook 版): 外部 YAML を廃し Notebook Parameter Block (大文字定数) を実験単位スナップショットとして Git 追跡 (セル差分)。
2) Pluggable Runner: ランタイム毎 (llama.cpp / vLLM / ExLlamaV2 / Transformers / 予備: TensorRT-LLM) を共通 Protocol に準拠。
3) Quantization Matrix: GGUF, GPTQ, AWQ, EXL2, bitsandbytes(8/4), FP16/BF16 を統一指標で横比較。
4) Progressive Tuning Ladder: Base → SFT → (LoRA | QLoRA | DoRA | P-Tuning v2) → ORPO → (RLHF PPO | KTO)。
5) Dual Evaluation: 機械指標 (latency, throughput, memory, JSON validity) + LLM Judge ベース品質 (win_rate 等)。
6) Composite Score = (品質指標正規化 * 重み) / コスト (時間 + VRAM + 電力推定)。
7) Strict Reproducibility: 乱数種/依存バージョン/モデルハッシュ/設定スナップショットをログ。

## 3. 想定ユースケース
- 同一モデルを vLLM vs llama.cpp vs ExLlamaV2 で量子化差含めベンチし最適推論条件を探索。
- LoRA / DoRA / P-Tuning v2 の収束速度・最終品質・VRAM 消費比較。
- ORPO / KTO / RLHF(PPO) 適用のコスト対効果 (追加品質 Δ vs 計算コスト)。
- 異タスク (例: 役割指示, JSON 構造生成, 関数/ツール提案) に対する prompt adapter 戦略評価。

## 4. 論理アーキテクチャ (Layers)
| Layer | 役割 | 主コンポーネント |
|-------|------|------------------|
| Interface | CLI / (将来)API | chat_cli, bench_infer, compare_models |
| Orchestration | 実験/ベンチ/学習制御 | ExperimentRunner, BenchmarkMatrix, TuningPipeline |
| LLM Abstraction | 推論 & ルーティング | BaseRunner, ConfidenceRouter |
| Prompt Adapter | タスク適応/テンプレート | PromptAdapter, TemplateStore |
| Quantization & Loading | 重みロード/最適化 | WeightLoader, CacheManager |
| Evaluation/Judge | メトリクス & LLM 判定 | MetricsCollector, JudgeClient, A/BComparator |
| Logging & Artifact | 再現性/保存 | RunLogger, Manifest, HashVerifier |
| Security & Policy | ライセンス/I/O制御 | LicenseGuard, IOPolicy, Sandbox |

## 5. ランナー抽象 & 実装方針
Protocol: `BaseRunner` (必須: load(), generate(prompt, params) -> tokens/stream, tokenize(), get_metrics()).
実装:
- `TransformersRunner`: HF Transformers 標準。bfloat16/flash-attn 対応。
- `VLLMRunner`: 連続バッチング/高スループット。ペンディングタスクと KV reuse を活用。
- `LlamaCppRunner`: 低メモリ/GGUF 用。grammar 拘束サポートを利用。
- `ExLlamaV2Runner`: GPTQ/EXL2 高速推論。分割ロードと pagesize 調整。
- (将来) `TensorRTRunner`: FP8/INT8 最適化 (別リポ or optional import)。
差異吸収: 共通 `GenerationParams` (temperature, top_p, top_k, repetition_penalty, max_new_tokens, stop, grammar(optional))。

## 6. モデル & 量子化サポート
ファイル: `configs/model.yaml` / `configs/quantization.yaml`
メタ項目: model_id, family, size_b (7B/14B/...), revision, license, trust_remote_code, preferred_runners.
量子化指定例:
```yaml
variants:
  - name: q4_gguf
    format: gguf
    bits: 4
    group_size: 128
    runner: llama_cpp
  - name: gptq_4bit
    format: gptq
    bits: 4
    sym: true
    runner: exllamav2
  - name: awq_int4
    format: awq
    bits: 4
    runner: transformers
baseline:
  - name: bf16
    format: bf16
    runner: vllm
```
ロードパス: :model_cache/:model_id/:variant/。ハッシュ検証 `sha256sum` 記録。

## 7. チューニングパイプライン
段階 (ステージング実行可能):
1) SFT: JSON/構造出力安定化 + 指示追従。LoRA ベースラインも同時生成。
2) 効率化適用: LoRA / QLoRA / DoRA / P-Tuning v2 (比較). 共通 AdapterInterface。
3) 指標最適化: ORPO (preference), KTO (ターゲット出力近似), RLHF(PPO) (政策更新)。
4) 合成データ増幅: 既存モデル + ルールでサンプル増 (品質フィルタ + dedup)。
5) 最終統合: 勝者アダプタ統合 or Ensemble Router へ反映。

構成ファイル (`configs/tuning.yaml`) 例:
```yaml
stages:
  - name: sft
    method: sft
    base_model: qwen2-7b
    dataset: data/sft/*.jsonl
    lora: false
    epochs: 2
  - name: lora_compare
    method: lora
    rank: [8,16]
    alpha: 16
    target_modules: [q_proj,k_proj,v_proj,o_proj]
  - name: dora
    method: dora
    rank: 8
  - name: orpo
    method: orpo
    preference_dataset: data/pref/*.jsonl
  - name: rlhf_ppo
    method: ppo
    reward_model: rm_qwen2_7b
    kl_coef: 0.02
```

## 8. プロンプト適応層 (PromptAdapter)
目的: タスク (会話/JSON 構造/ツール提案/分類/評価) に応じたテンプレート切替 & 共通出力正規化。
抽象: `PromptAdapter.build(context: PromptContext) -> PromptBundle(system, user, fewshots, constraints)`。
特徴:
- Jinja2 テンプレート + フィルタ (indent_json, truncate_tokens)。
- Constraint Injection: JSON schema / function signatures を system 部に組み込み。
- Self-check Tag: 出力末尾に <SELF_CHECK/> 指標生成指示を付加 (任意)。

## 9. コンフィグ体系 (MVP Notebook 版)
MVP では外部設定ファイルは必須とせず、Notebook Parameter Block に統合する。将来拡張時に下表を段階導入。
| (将来)ファイル | 目的 | 主フィールド |
|----------|------|--------------|
| model.yaml | モデル共通メタ | model_id, family, context_length, preferred_runners |
| quantization.yaml | 量子化バリアント | variants[], baseline[] |
| runtime.yaml | ランタイム調整 | batch_size, tensor_parallel, gpu_mem_fraction |
| decode.*.yaml | サンプリングプリセット | temperature, top_p, repetition_penalty |
| benchmark.yaml | ベンチ指標/繰返 | scenarios, repeats, warmups, metrics |
| tuning.yaml | チューニングステージ | stages[], artifacts_out |
| judge.yaml | LLM Judge 設定 | judge_model, prompt, criteria, max_pairs |
| datasets.yaml | データセット管理 | alias→path/glob, type(train|eval|pref) |
| security.yaml | I/O / ライセンス | allow_network, allowed_hosts, license_policy |
| functions.json | (任意) 構造出力用ツールスキーマ | name, parameters, risk |

## 10. ベンチマーク & メトリクス (2025-10-09 追記)
低レベル:
- latency_ms (prefill, decode), throughput_tok_s (prefill/decode), tps_std, load_time_ms
- peak_vram_mb, peak_ram_mb
構造品質:
- json_valid_rate, function_call_valid_rate, output_length_tokens
モデル品質 (タスク依存):
- exact_match, rougeL / bleu, pass@k, structured_accuracy (旧 function_call_valid_rate), hallucination_rate
Judge 指標:
- win_rate, tie_rate, quality_score_avg
複合スコア例:
`score = (win_rate * 0.5 + json_valid_rate * 0.2 + structured_accuracy * 0.3) / (latency_ms_norm * cost_coef)`

正規化参照: `configs/composite_norms.yaml` に (min,max) または reference_model を記録。capture_baseline スクリプトで初回生成。
token_count_source: すべて CanonicalTokenizer 基準 (Runner 内部と差異 >1% で WARNING)。tps_decode / tps_prefill に命名統一。

## 11. LLM Judge / A-B 比較
手法: GPT-4 class / Claude / 自己モデル cross-eval。ペアプロンプト (A,B シャッフル) に対し JSON 評点出力。
形式例 (judge.yaml):
```yaml
judge_model: gpt-4o
criteria:
  - instruction_following
  - correctness
  - format
  - brevity_penalty
max_pairs: 200
tie_threshold: 0.05
```
ログ: `judge/` に (pair_id, model_a, model_b, winner, scores, prompt_hash)。

## 12. ルーティング (ConfidenceRouter)
初期: 単純信頼度 (構造妥当性 + repetition ratio + length penalty)。
将来: 事前軽量評価器 (small classifier) / online bandit。閾値越えで上位モデル再生成。
設定 (model.yaml 内 router): primary, fallback, confidence_threshold, max_hops。

## 13. ロギング / 再現性 / アーティファクト (2025-10-09 追記 / Notebook 反映)
ランディレクトリ: `results/` 内サブフォルダ
- runs/: 実験単位 JSONL (header 行に config digest)。
- bench/: ベンチ集計 CSV/Parquet。
- judge/: LLM 判定ログ。
- tuning/: 学習チェックポイント/LoRA アダプタ。
- artifacts/: 量子化生成物、converted weights、manifest。
再現キー: commit_sha, utc_start, seed, model_variant, decode_preset, env (python pkgs hash), config_hash (Notebook Parameter Block 由来)。
ハッシュ: 重み/トークナイザ/データセットファイルを manifest で追跡 (FR-15 拡張)。欠損時 fail-fast。tokenizer.files (tokenizer.model, vocab.json, merges.txt, tokenizer.json 等) を `tokenizer` セクションに列挙し sha256 検証。
ログ追加フィールド例: tokenizer_name, token_diff_pct。

## 14. セキュリティ & コンプライアンス
- ライセンスフィルタ: 許可ライセンス (apache-2.0, mit, bsd-3) 以外使用時に警告。
- ネット遮断オプション & allowlist。
- 生成物 (チェックポイント/量子化) の法的再配布フラグ記録 (redistributable: true/false)。
- 機密入力キャッシュ禁止 (PII フラグ)。

## 15. ディレクトリ構成 (MVP Notebook 簡略)
```
notebooks/
  pipeline.ipynb  # 単一 Notebook (Parameter Block / 学習 / 量子化 / serve / ベンチ)
scripts/          # 補助的単体スクリプト (任意: train_qlora.py など既存資産)
results/          # bench.csv, adapter, merged, awq, logs
utils/            # config_utils.py (Parameter 収集と hash), metrics_utils.py
```
将来フル設計へ移行時は従来構成 (app/, configs/, data/...) を再導入。

## 16. スクリプト (最小 MVP → 拡張)
MVP:
- bench_infer.py: 単一モデル/プリセットの n 回計測。
- bench_matrix.py: (model × quant × decode) グリッド実行。
- compare_models.py: A/B/C 出力収集 + Judge リクエスト生成。
- chat_cli.py: 手動動作確認 & latency サンプル。
- quantize_gptq.py / quantize_awq.py / quantize_awq.py: 量子化支援。
- train_sft.py, train_lora.py, train_dora.py, train_ptuning.py
- train_orpo.py, train_kto.py, train_rlhf_ppo.py (後期)
- judge_eval.py: Judge 呼び出し & win_rate 集計。
- aggregate_runs.py: runs/ からメトリクス抽出し CSV。
拡張:
- cost_dashboard.py: 複合スコア計算 + markdown レポート。
- export_report.py: HTML/Markdown CI 用結果まとめ。

## 17. API 方針 (オプション / Capability degrade)
当面 CLI 主体。軽量 FastAPI を残し `/chat` + `/health` のみ (外部統合用)。
Capability Degrade Policy: 要求機能 (grammar/json_mode/lora/stream/actions) が未サポート Runner だった場合の動作を一元管理し、strict モードでは 400 (HTTP) かエラーを返却。structured_actions (ActionExecutor) は将来 /actions エンドポイントで実行可能な拡張点とする。

## 18. テスト戦略 (tokenizer consistency 追記)
階層:
1) Unit: PromptAdapter, Metrics, Router (閾値/フォールバック)、JsonStreamGuard。
2) MockRunner: 疑似 token レイテンシ分布・構造エラー挿入でルータ/修復動作試験。
3) Integration (tiny model, e.g. distilgpt2 / phi-2 4bit) で end-to-end JSON 有効性率を測定 (fast モード max_new_tokens=32)。
4) Regression: 代表シナリオ snapshot 出力の hash 比較。
5) Tokenizer Consistency: 全 Runner tokenize 長さ差異 <1% (50 サンプル) 閾値検証。
メトリクス検証閾値例: json_valid_rate >= 0.95 (モック), fallback_rate < 0.25。

## 19. エラー処理 / リトライ
- JSON 不整合: incremental 修復 → 局所再サンプル (max 2) → フォールバックモデル。
- Judge API 失敗: エクスポネンシャルバックオフ (3) → 部分 win_rate 除外扱い。
- 量子化ロード失敗: キャッシュ削除 + 再試行 1 回 (整合性ハッシュ不一致時)。

## 20. リスク & 緩和
| リスク | 影響 | 緩和 |
|--------|------|------|
| Colab セッション時間切れ | 長時間学習中断 | チェックポイント頻度短縮 (N ステップ) |
| VRAM 不足 | 学習失敗 | gradient_checkpointing + offload + 低ランク LoRA |
| Judge コスト増 | 予算圧迫 | サンプリング縮小 + 低価格 judge モデル混成 |
| ライセンス不整合 | 再配布不可 | manifest + license_guard で fail-fast |
| 出力フォーマット崩壊 | 評価指標無効 | grammar/stream guard + self-heal 再試行 |

## 21. マイルストーン (改訂 + tokenizer)
- M1: ランナー抽象 + bench_infer + logging 再現情報 + Manifest tokenizer ハッシュ + TokenizerRegistry 基礎。
- M2: quantization matrix + bench_matrix + composite metric 初期版 + composite_norms capture。
- M3: SFT + LoRA/QLoRA/DoRA/PromptAdapter 比較 + judge 基盤。
- M4: ORPO / KTO / RLHF(PPO) の一種実装 + cost_dashboard。
- M5: ルーティング最適化 (bandit) + レポート自動生成 + ライセンス/セキュリティ完成。

## 22. 今後の拡張候補
- TensorRT-LLM / FlashDecoding 集約
- Multi-GPU (tensor / pipeline parallel) 対応
- Streaming Diff / Edit 構造生成
- Reward モデル内製 (small transformer) & self-training ループ
- Web UI ダッシュボード (gradio / streamlit)

## 23. 旧設計からの主差分
- NPC 専用語彙/世界観依存要素を削除し汎用タスク指向へ。
- Function/Action 出力は任意タスクの structured JSON へ一般化。
- 評価軸を「生成品質 + コスト/性能 + 再現性」三位一体に再編。
- チューニング手法群 (DoRA, P-Tuning v2, ORPO, KTO, RLHF) の段階定義追加。

---
本基本設計 (v2) を基に、次工程で: (a) Runner 実装スケルトン拡充, (b) bench_infer.py / bench_matrix.py 作成,
(c) PromptAdapter プロト, (d) tuning.yaml スキーマ確定, (e) 主要テスト (MockRunner) 整備 を着手する。

