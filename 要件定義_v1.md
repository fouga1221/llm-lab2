# 要件定義 v1: Qwen3-14B LoRA/QLoRA 微調整 & 推論ランタイム比較実験基盤

作成日: 2025-10-09  
対象環境: Google Colab (L4 GPU) / ローカル検証 (任意)  

## 1. 背景 / 目的 / 範囲
**目的**: Qwen3-14B を LoRA および QLoRA (4bit) で微調整し、Transformers (fp16) と vLLM (+AWQ 量子化) の単発リクエスト推論性能を比較・記録する簡易実験基盤を整備する。

**比較軸 (主要指標)**:
1. E2E 遅延 (リクエスト送信→最終トークン受領)
2. デコード性能: ms/token (= decode_ms / 出力トークン数), tokens/s
3. GPU メモリ使用量 (ピーク)

**範囲**:
- Qwen3-14B ベースモデル 1 種 (派生 variant: LoRA, QLoRA, AWQ 量子化後)
- LoRA/QLoRA 学習・merge・量子化・serve・ベンチを単一 Colab Notebook 内で完結
- 単発 (single request) ベンチ: prompt 長 (Short/Mid/Long) × ランタイム (Transformers fp16 / vLLM AWQ) × 反復
- Notebook セルの Parameter Block (大文字変数) を唯一の設定ソースとし外部 YAML/JSON を使用しない

**非範囲** (Out of Scope):
- 分散 (multi-GPU) 学習 / inference
- RLHF / ORPO / その他高度ファインチューニング
- 多モデル自動 routing / エージェント機能
- マルチユーザ Web UI
- 長期継続サーバ監視 / オートスケール

## 2. 利用ペルソナ
| 役割 | 目的 | 成功指標 |
|------|------|----------|
| モデル実験者 | LoRA/QLoRA 設定と量子化効果の把握 | VRAM 削減率 / ms/token 改善 |
| 推論エンジニア | vLLM + AWQ の実用閾値評価 | E2E 遅延と fp16 同等性 |
| レポート作成者 | CSV 出力による比較表作成 | 欠損値無し / 再現ログ一致 |

## 3. ユースケース (代表)
1. LoRA 学習設定 A/B を 1 epoch で試し学習ログと VRAM 使用を記録
2. 学習済み LoRA をベースに merge → fp16 モデル生成
3. fp16 モデルを AWQ で 4bit 量子化
4. vLLM Docker で AWQ モデルを serve し単発リクエスト測定
5. Transformers fp16 推論と vLLM AWQ を同一プロンプト条件で 5 回反復し中央値比較
6. CSV をダウンロードし差分 (ms/token, VRAM) を表形式で報告

## 4. データ仕様
入力データ形式: JSONL (1 行 1 サンプル)
```
{"messages": [
  {"role": "system", "content": "You are a helpful assistant."},
  {"role": "user", "content": "<ユーザ質問>"},
  {"role": "assistant", "content": "<参考回答 (SFT の場合)>"}
]}
```
- 学習 (SFT) 用: system+user+assistant が揃う行
- 推論ベンチ用: system+user のみ (assistant 欄なし)
- 追加前処理 (任意): CSV などを JSONL へ整形するユーティリティ (拡張余地)
- 文字コード: UTF-8 / 改行コード LF

## 5. 学習要件 (QLoRA 例)
| 項目 | 設定 (初期) |
|------|-------------|
| 量子化読込 | load_in_4bit=True, quant_type=nf4, double_quant=True, compute_dtype=bf16 |
| LoRA Hyper | r=16, alpha=32 (α/r=2), dropout=0.05 |
| 対象モジュール | [q,k,v,o,up,down,gate] (実際はQwen層の該当線形名に合わせマッピング) |
| 省メモリ | gradient_checkpointing=True, optim=paged_adamw_8bit, packing=True |
| 長さ/バッチ | max_seq_len=2048, per_device_batch=1, grad_accum=8 |
| 反復 | 1–2 epoch (early stop 可) |
| 評価ステップ | (任意) eval_steps=N / patience=M |

出力アーティファクト:
- LoRA adapter (PEFT フォルダ)
- 学習ログ (loss / lr / VRAM ピーク / commit sha)

### 5.1 target_modules 最終確認 (必須実行手順)
Qwen3 系では以下 7 モジュールが通例: `["q_proj","k_proj","v_proj","o_proj","up_proj","down_proj","gate_proj"]`

検証スニペット (Colab で 1 回実行し末尾一致名を採用):
```python
import torch, re
from transformers import AutoModelForCausalLM
model_name = "Qwen/Qwen3-14B"
m = AutoModelForCausalLM.from_pretrained(model_name, trust_remote_code=True)
names = [n for n, mod in m.named_modules() if isinstance(mod, torch.nn.Linear)]
pattern = re.compile(r"(q_proj|k_proj|v_proj|o_proj|up_proj|down_proj|gate_proj)$")
hits = [n for n in names if pattern.search(n)]
for h in hits: print(h)
```
出力末尾セグメントに差異 (例: `attention.q_proj`) があれば `target_modules` にその末尾名を列挙。正規表現指定例: `r"(?:q_proj|k_proj|v_proj|o_proj|up_proj|down_proj|gate_proj)$"`。

## 6. 量子化 & 配備フロー
1. LoRA 学習 (QLoRA or LoRA)
2. LoRA を fp16 に merge (ベース + delta)
3. AWQ 量子化 (llm-compressor / awq_cli 相当) → 4bit 重み生成
4. vLLM Docker 起動 (OpenAI 互換エンドポイント) 例: `/v1/chat/completions`
5. (オプション) 複数 LoRA を vLLM の --lora <path> でホットスワップ

### 6.1 AWQ 量子化 詳細 (LLM Compressor 推奨)
**ツール**: `llmcompressor` (vLLM 公式系)
```
pip install llmcompressor
```
最小 oneshot 例:
```python
from llmcompressor import oneshot
from llmcompressor.modifiers.quantization import AWQModifier
recipe = [AWQModifier(scheme="W4A16", targets="Linear", ignore=["lm_head"])]
oneshot(
    model="/path/to/merged-fp16",
    dataset="open_platypus",  # または内部校正データ
    recipe=recipe,
    output_dir="/path/to/model-awq",
    max_seq_length=4096,
    num_calibration_samples=512,
)
```
出力はそのまま vLLM の `--model` で利用可。LoRA→merge→AWQ の順序厳守 (量子化後の追加 LoRA 適用は対象外)。

#### 代替: AutoAWQ
```
pip install autoawq
python examples/cli.py --hf_model_path /path/to/merged-fp16 \
  --quant_name model_awq --local_save_path /out --zero_point --device_map cuda
```

### 6.2 vLLM 起動 (AWQ)
```
python -m vllm.entrypoints.api_server \
  --model /path/to/model-awq --quantization awq --dtype half \
  --max-model-len 4096 --gpu-memory-utilization 0.9
```
最新版 vLLM を推奨。`--quantization awq` 指定不可の場合はバージョン不整合を疑う。

## 7. ベンチマーク条件
| パラメータ | 値 |
|------------|----|
| Prompt 長カテゴリ | Short ≤512 tok / Mid ≈1024 tok / Long ≈2048 tok |
| 出力上限 | max_new_tokens=256 |
| 生成設定 | temperature=0.7, top_p=0.9, repetition_penalty=1.1 |
| 反復 | 各条件 5 回 (初回=ウォームアップ除外可) |
| ランタイム | Transformers(fp16), vLLM(AWQ) |

収集メトリクス:
- ttft_ms (Time To First Token)
- e2e_ms (送出→完了)
- decode_ms (= e2e - ttft)
- out_tokens
- ms_per_tok (= decode_ms / out_tokens)
- tok_per_s (= out_tokens / (decode_ms/1000))
- vram_peak_gb (torch.cuda.max_memory_allocated / nvidia-smi 確認値)
- commit_sha, versions (transformers, vllm, peft, trl, torch, python)
- prompt_tokens (生成前入力長; tokenizer.chat_template で計測)
- total_tokens (= prompt_tokens + out_tokens)

## 8. 成功基準 (Acceptance 初期)
1. 全条件 (Short/Mid/Long × ランタイム) で OOM なし
2. vLLM+AWQ の ms_per_tok が Transformers(fp16) 比 +20% 以内 (Long のみ +30% まで許容)
3. vLLM+AWQ が VRAM ピークで ≥25% 削減 あるいは e2e_ms (中央値) が fp16 と同等 (±5%)
4. CSV に欠損値なし & 再実行 (同 seed) で中央値差 <10% (Short 条件)

## 9. 機能要件 (Functional Requirements)
| ID | 要件 | 説明 |
|----|------|------|
| FR-1 | LoRA/QLoRA 学習処理 | Notebook Parameter Block を直接参照し学習を実行 |
| FR-2 | LoRA merge ユーティリティ | LoRA 差分を fp16 ベースへ統合 |
| FR-3 | AWQ 量子化スクリプト | merge 後モデルを 4bit 重みへ変換 |
| FR-4 | vLLM サーブ起動ラッパ | Docker / サービス起動 & ヘルスチェック |
| FR-5 | 単発ベンチスクリプト | 指定プロンプト/設定で 5 回計測 & 集計 |
| FR-6 | メトリクス収集 & CSV 出力 | 既定列を追記 (append or overwrite) |
| FR-7 | VRAM ピーク計測 | torch + nvidia-smi 併用 (欠損時 fallback) |
| FR-8 | バージョン/commit 記録 | 主要ライブラリ & git commit 取得 |
| FR-9 | 再現 seed 設定 | random/torch/cuda seed 固定 |
| FR-10 | コンフィグ差分記録 | Notebook 内大文字変数集合を正規化→sha256 先頭16桁を `config_hash` として CSV に出力 |
| FR-11 | target_modules 自動検証 | スニペット実行結果から不足/揺れ検出 (警告) |
| FR-12 | prompt_tokens 計測 | chat テンプレート適用後の入力トークン数算出 |

## 10. 非機能要件 (NFR)
| ID | 要件 | 指標 / 目安 |
|----|------|--------------|
| NFR-1 | 再現性 | 同一 seed / データで中央値差 <10% (Short) |
| NFR-2 | 可搬性 | Colab 1 ノートブックで全流れ完結 (追加インストール最小) |
| NFR-3 | 可観測性 | 単一 CSV + 学習ログで全指標確認可能 |
| NFR-4 | 性能計測信頼性 | ttft, e2e, decode の計測誤差 <5ms (計測関数内連続2回差) |
| NFR-5 | エラー検知 | 量子化/サーブ失敗は非 0 exit + 明示ログ |
| NFR-6 | 再現性シード徹底 | seed 固定 (python/numpy/torch/cuda) & vLLM per-request seed 指定 |
| NFR-7 | 計測列拡張容易性 | 新メトリクス列追加が既存 CSV 破壊しない (後方互換) |
| NFR-8 | 依存管理方針 | requirements.txt でマイナー範囲指定 (>=x.y,<x.(y+1)) とし、量子化/サーブ系は最新追随。追加時は利用目的・代替不可理由・影響を PR 記載 |

## 11. コンフィグ管理 / 入出力 (Notebook 方針)
外部設定ファイルを廃止し、Notebook 冒頭に Parameter Block を置く。例:
```python
# ==== PARAMETERS ====
BASE_MODEL = "Qwen/Qwen3-14B"
LORA_R = 16
LORA_ALPHA = 32
LORA_DROPOUT = 0.05
USE_QLORA = True
SEQ_LEN = 2048
PER_DEVICE_BATCH = 1
GRAD_ACCUM = 8
EPOCHS = 1
LR = 2e-4
CALIB_SAMPLES = 64
CALIB_MAX_LEN = 512
BENCH_PROMPT_LENGTHS = ["short","mid","long"]
BENCH_REPEATS = 5
MAX_NEW_TOKENS = 256
TEMPERATURE = 0.7
TOP_P = 0.9
SEED = 42
```
`config_hash` は上記大文字変数を dict 化→JSON ソート→sha256。
成果物: adapter / merged / awq / bench.csv (CSV 末尾列に hash, seed)。

## 12. ログ / 計測方法概要
| 項目 | 方法 |
|------|------|
| ttft_ms | generate() 開始→最初のトークン受領時刻差 |
| e2e_ms | リクエスト送信→完了時刻差 |
| vram_peak_gb | torch.cuda.max_memory_allocated()/1e9, fallback: nvidia-smi parse |
| commit_sha | git rev-parse HEAD (失敗時 unknown) |
| versions | importlib.metadata 取得 |
| prompt_tokens | tokenizer.apply_chat_template(messages, add_generation_prompt=True) で encode 長 |
| total_tokens | prompt_tokens + out_tokens |

## 13. リスクと緩和策
| リスク | 影響 | 緩和 |
|--------|------|------|
| Colab セッション切断 | 学習途中停止 | 早期 epoch / adapter 逐次保存 |
| OOM (Long prompt) | 実験成立せず | QLoRA + grad_accum + seq_len 調整 |
| AWQ 品質劣化 | ベンチ不公平 | fp16 baseline 先行測定 & 比較ログ保持 |
| vLLM 未対応設定 | 起動失敗 | 既知対応パラメータのみ使用 & バージョン固定 |
| 計測誤差 (ウォームアップ) | 指標歪み | 1回ウォームアップ廃棄 |
| target_modules 抽出失敗 | LoRA 効果低下 | 正規表現検証 + 欠損時ログ警告 |
| 再現seed不一致 | 比較不能 | seed 固定コード共通化 & vLLM seed 指定 |

## 14. マイルストーン / フェーズ
| M | 内容 | Exit Criteria |
|---|------|---------------|
| M1 | QLoRA 学習 + merge + AWQ 量子化 | LoRA adapter & 量子化重み生成成功 |
| M2 | vLLM サーブ & 単発ベンチ | Short/Mid/Long 5回計測 CSV 出力 |
| M3 | 成功基準評価 & レポート | 成功基準 1–3 満たし CSV 提出 |

## 15. 受入基準 (Acceptance)
- Notebook 方式 FR-1〜FR-10 の最小実装動作確認
- 成功基準 (8.1〜8.3) 達成 (ログエビデンス)
- ベンチ CSV に全列あり・欠損なし
- target_modules 検証で欠落 0 (もしくは警告リスト提示) 
- prompt_tokens/total_tokens 列が埋まる
- 設定差分ハッシュ記録 (再実行で一致)

## 16. 変更管理
- 軽微パラメータ変更 (batch, rank): PR ラベル `config-tweak`
- 手順フロー変更 (追加段階、量子化方式追加): Issue で合意後 v1.x → v1.y
- 破壊的変更 (指標列削除/名称変更): version を v2.0 へ昇格

## 17. 成果物一覧
- `train_lora.py`, `train_qlora.py`
- `merge_lora.py`
- `quant_awq.py`
- `serve_vllm.sh` (または notebook セル)
- `bench_single.py`
- `inspect_target_modules.py` (optional helper)
- `configs/*.yaml`
- `results/bench/bench.csv`
- LoRA adapter / 量子化済み重み

## 18. オープン課題 / 将来拡張 (任意)
- 複数 LoRA rank sweep 自動化
- プロンプト生成パターン増加 (JSON スキーマ/ツール呼び出し)
- 並列ベンチ (複数同時リクエスト) でのスループット測定
- Diff レポート (ms/token 改善比) 自動 Markdown 生成
- vLLM multi-process 再現性評価 (環境変数: VLLM_ENABLE_V1_MULTIPROCESSING=0)
- AWQ 高度設定 (mixed group size, per-channel) の比較

## 19. 再現性設定 (付録)
### 19.1 共通 seed 固定コード
```python
import os, random, numpy as np, torch
def set_global_seed(seed=42):
  os.environ["PYTHONHASHSEED"] = str(seed)
  random.seed(seed)
  np.random.seed(seed)
  torch.manual_seed(seed)
  torch.cuda.manual_seed_all(seed)
  torch.backends.cudnn.deterministic = True
  torch.backends.cudnn.benchmark = False
  # 任意: 完全決定論, 性能低下留意
  # torch.use_deterministic_algorithms(True)
  # os.environ["CUBLAS_WORKSPACE_CONFIG"] = ":16:8"  # or ":4096:8"
```

### 19.2 vLLM 再現性メモ
- SamplingParams(seed=...) を明示設定
- エンジン初期化時 seed 指定 (対応バージョン)
- マルチプロセス無効化 (例: `VLLM_ENABLE_V1_MULTIPROCESSING=0` if applicable)
- 完全一致は保証困難: 乱数・スケジューラ差で ±少量トークン速度揺らぎ許容

---
(以上)
