# AGENTS ガイドライン

本ドキュメントは、このリポジトリにおけるエージェント（AI/自動化スクリプト/開発補助ツール等）が守るべき運用方針・プロセスを定義します。すべて **日本語** での対応を前提とします。

---
## 1. 基本原則
1. 返答・コメント・自動生成物は原則すべて日本語で記述する。
2. 実装は必ず最新の設計書（`設計.md`, `GUI設計.md` など該当文書）に従う。
3. 設計書が参照されていない・存在しない・曖昧な場合、開発を進める前に不足点を質問して確認する。
4. 指示内容が設計書と異なる、あるいは設計書の適用範囲外である場合：
   - まず差分を明示し、設計書更新が必要である旨を通知。
   - 小規模変更であればエージェントが設計書を更新し、その更新内容を示した上で実装を継続。
   - 大幅変更（影響範囲が広い / 既存アーキテクチャの再設計が必要）の場合は、必ずユーザー確認を得てから着手。

---
## 2. 事前調査 (Pre-Implementation Survey)
実装着手前に以下を必ず確認・整理する：
- 対象機能の目的 / 成果物 / 入出力仕様
- 関連する既存モジュール・クラス・関数（再利用可能なものの有無）
- 依存設定ファイル（`config.ini`, `.spec`, `Pipfile` 等）への影響
- 既存のログ/エラーハンドリング方針（`logger/` 配下など）
- 並列処理・プロセス間通信（multiprocessing, Queue, Manager 等）の影響有無
- 例外/フェイルパターン/リカバリ戦略

調査結果は簡潔な形で「実装手順」セクションに落とし込み、手順化する。

---
## 3. 実装手順の標準フロー
1. 要件確認／不足点の明確化
2. 事前調査（上記チェック項目の網羅確認）
3. 設計書との差分確認（必要なら更新案提示 → 承認/自己承認ルールに従い更新）
4. 具体的な実装手順リスト化（粒度：5〜15ステップ目安）
5. 影響範囲の安全策（バックアップ／別ブランチ／段階的コミット方針）
6. コード実装（小さく検証）
7. 静的検査（型チェック / Lint 相当。プロジェクトが未整備の場合：簡易自己レビュー）
8. 動作検証（最小ユースケース / エッジケース）
9. ログと例外メッセージ整備（ユーザー向け / 開発者向けの粒度分離）
10. 変更概要と利用方法の共有（README / CHANGELOG / 設計書への反映）

---
## 4. 設計書更新ポリシー
| ケース | 対応 | 承認要否 |
|--------|------|----------|
| 軽微な引数追加 / オプション追加 | 設計書内該当章へ追記 | 不要（後で差分報告） |
| 新しいモジュール追加 | 目的 / 公開API / 依存関係を追記 | 場合により要確認 |
| 既存アーキテクチャ変更（クラス責務再編 等） | 影響分析 + Before/After 図 | 要確認 |
| 重大リネーム / 永続データ形式変更 | マイグレーション手順必須 | 要確認 |

---
## 5. コードスタイル & 品質指針
- 再利用性：重複ロジックは抽象化（共通関数 / Utilityモジュール）。
- 命名：英語、一貫性（device, trigger, logger 等の語を統一）。
- 早期 return / ガード節でネストを浅く。
- 例外：捕捉する例外型を限定し、再 raise かログ出力方針を明確化。
- ロギング：`init_logging()` で初期化された logging を利用し、print は撤廃（必要なら redirect_print）。
- 並列処理：共有状態アクセスは極力局所化し、データ受け渡しは Queue / immutable データに寄せる。
- マジックナンバー排除：意味のある定数名で定義。

### ログ出力レベル運用例
| レベル | 用途例 |
|--------|--------|
| DEBUG | プロセス間通信内容、詳細な分岐結果 |
| INFO  | 起動/終了、主要操作成功、モード選択結果 |
| WARNING | リトライ可能エラー、想定内フォールバック |
| ERROR | 実行継続は可能だが機能失敗 |
| CRITICAL | 重大障害（プロセス停止） |

---
## 6. 新規関数追加時のチェックリスト
- 既存の同等機能が無いか検索（grep / semantic）。
- 単一責務か？（複数責務なら分割を検討）
- 引数と戻り値の型・意味が明確か？
- 例外 / エラー戻りのパターンを記述したか？
- ログは適切なレベルか？
- ユニット or 簡易テスト可能な構造か？（I/O, 時間依存, グローバル状態を避ける）

---
## 7. 大幅設計変更の判定基準
以下のいずれかを満たす場合は「大幅変更」とし、事前承認必須：
- 既存クラスの責務を 30%以上書き換える
- 公開 API (import されるクラス/関数) 名称変更
- 永続ファイル/プロトコル形式の互換性破壊
- 複数プロセス（multiprocessing）間の通信モデル変更
- ビルド/配布構成（PyInstaller spec 等）へのインパクト

---
## 8. フォールバック / リスク軽減
- 段階的コミット：大きな変更はフェーズ分割（クラス追加 → 旧呼び出し互換層 → 呼び出し移行 → 旧コード削除）。
- 互換エイリアス：リネーム時は旧名を一定期間 alias 化（例：`LoggerProcess = TriggerLoggerProcess`）。
- ログで deprecation 通知：WARNING レベルで告知し、削除予定バージョンを明示。

---
## 9. 既存ログ／トリガ処理との整合
- トリガ専用プロセス：`TriggerLoggerProcess` を中核とし、トリガイベント群のみを責務とする。
- 一般アプリログ：`init_logging()` 初期化後は標準 logging を利用。
- print 使用禁止（`redirect_print=true` の設定を用いる場合を除く）。

---
## 10. 実装報告テンプレート
```
# 変更概要
- 対象: <モジュール/機能>
- 種別: 機能追加 / 修正 / 改善 / リファクタ / ドキュメント / その他

## 背景 / 目的
<理由>

## 実装内容
- <箇条書きで要点>

## 設計書差分
- 更新ファイル: <一覧>
- 主な差分: <概要>

## 動作確認
- 手順:
- 結果:

## 影響範囲
<他モジュール, ビルド, 配布 など>

## 今後の課題
<任意>
```

---
## 11. 運用ルール（エージェントワークフロー）簡易フロー
1. 指示受領 → 要件正規化
2. 設計書参照 / 更新要否判定
3. 事前調査まとめ
4. 手順リスト化 & 合意
5. 実装（小刻み / 冪等意識）
6. 検証（最小 & エッジ）
7. ログ/例外/ドキュメント整備
8. サマリ報告 / 次の改善提案

---
## 12. 今後の拡張候補
- 自動テスト雛形生成スクリプト
- Lint / Type Check の CI 組み込み
- PyInstaller ビルド検証の自動ワークフロー
- ログレベル動的切替用コマンド（IPC 経由）

---
最終更新日: 2025-09-25
